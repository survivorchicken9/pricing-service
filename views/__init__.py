# controllers in this app just give user HTML and receive data
# so that they can present more HTML, so there is very little processing going on
# as such controllers in this app are just referred to as "views" because they just serve the views
